import Layout from "@/components/layouts/Layout";
import ManagedService from "@/components/pages/level1/ManagedService";
import React from "react";

const page = () => {
  return (
    <Layout>
      <ManagedService />
    </Layout>
  );
};

export default page;
